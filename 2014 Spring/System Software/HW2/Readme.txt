This program will compile from gcc and takes the code input file as an argument. If you wish to save this to a text file please use Bash's ">" operator.

To compile:
gcc -o LexemeAnalyzer.o LexAn.c

To execute:
./LexemeAnalyzer.o <YOUR CODE FILE>

Refer to test output.txt for the sample program and output.
